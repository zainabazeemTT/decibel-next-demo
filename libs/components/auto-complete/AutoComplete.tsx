import { AutoCompleteProps } from '@app/types'
import { AutoComplete as AntAutoComplete } from 'antd'

export const AutoComplete: React.FC<AutoCompleteProps> = (props) => <AntAutoComplete {...props} />
